import 'package:flutter/material.dart';

class SearchResultInfoSection extends StatelessWidget {
  const SearchResultInfoSection({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Founded : 0');
  }
}
