import 'package:flutter/widgets.dart';

class SpecialTagsPage extends StatelessWidget {
  const SpecialTagsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(child: Text('Tags'));
  }
}
