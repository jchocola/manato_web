import 'package:flutter/material.dart';

class TagInfoPage extends StatelessWidget {
  const TagInfoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Tag Info Page')) , body: webBody(context),);
  }

  Widget webBody(BuildContext context) {
    return Column(
      children: [

      ],
    );
  }
}
