import 'package:flutter/material.dart';

class EidtTemplatePage extends StatelessWidget {
  const EidtTemplatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Edit template'),),
      body: Placeholder(),
    );
  }
}